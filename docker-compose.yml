version: "3.9"

services:
  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build: ./Frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
